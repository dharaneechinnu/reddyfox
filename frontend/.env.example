# Use localhost for browser access from this same machine, or this machine's
# LAN IP (e.g. http://192.168.1.4:8000/api) so phones/other devices on the
# network can reach the backend too — "localhost" on a phone means the phone.
VITE_API_BASE_URL=http://localhost:8000/api
